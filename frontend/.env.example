# URL of the NestJS signaling backend (Socket.IO).
# Leave empty to use the same origin that served the page (production build
# served by NestJS). In dev, point it at the backend port.
VITE_SIGNAL_URL=http://localhost:4000
